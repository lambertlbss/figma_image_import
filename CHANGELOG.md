# 更新记录

本文件用于持续记录插件的功能调整、问题修复、性能优化、资源更新和验证结果。

## 维护规则

- 每次修改代码、界面、文档或资源时，同步更新本文件。
- 尚未提交的修改记录在“未发布”中。
- 提交或发布时，将“未发布”内容整理为带日期的版本条目。
- 每条记录按需使用：新增、修改、修复、性能、文档与资源、验证。
- 记录用户可感知的结果，并注明重要兼容行为和迁移方式。

## [未发布]

### 新增

- 组件集分类面板增加“刷新页面”按钮，可复用当前 manifest 重新扫描 Figma 页面，无需重新选择本地目录或计算图片哈希。
- 新增独立“变动预览”页面，点击新增、更新、删除、移动或未变化统计卡片即可进入对应明细。
- 预览页支持全部变动、各变动类型、已选一级文件夹和资源名/路径筛选；更新显示前后尺寸，移动显示前后路径，删除显示执行原因。
- 预览页底部可返回同步设置，也可直接确认并开始同步。
- 变动预览增加“组件集”页签，只汇总同步完成后确实受影响的 Component Set，并展示其最终成员。
- 组件集以标题折叠面板呈现且默认收起；展开后显示内部资源名称、尺寸、变动状态和本地图片缩略图。
- 变动预览增加“异常资源”页签，覆盖全部有效本地 PNG，不受同步文件夹勾选状态影响；按体积从大到小展示文件大小、图片尺寸、单位像素体积、同尺寸中位数、元数据占比和异常原因。
- 新增 PNG 区块检查，可识别 `iTXt`、`tEXt`、`zTXt`、`eXIf`、`iCCP` 等可能导致导出文件异常膨胀的元数据。

### 修改

- 切换严格尺寸与 AI 辅助分类时，会先重新扫描当前 Figma 页面索引，再重新计算同步差异。
- AI 分类请求会携带已有 Component Set 名称及改名策略；只有纯 `宽x高` 尺寸占位名允许 AI 重命名，其他语义名称在读取和执行方案时都会被强制保留。
- AI 分类请求增加纯尺寸名称优化候选；成员完全不变的 `404x404` 类组件集也会被要求单独审核命名，可靠语义不足时允许保留原名。
- 仅优化组件集名称时复用原 Component Set 存储键和节点，不重新导入图片、不拆组，也不更换 Component Set 或内部 Component ID。
- AI 分类增加组件尺寸一致性标准：同组线性尺寸比例上限调整为 `1.7506`，取自组件库 AJ 的 `common` Section 中现有 Component Set 的最大实际差距；规则同时写入 MCP 请求并由插件在读取方案时强制校验。
- 删除形状、宽高比和视觉轮廓限制，避免 AI 因定义歧义错误拆组。
- 同步计划明细增加当前尺寸、原尺寸和仅排版修复标记，供变动预览准确解释更新与移动。
- 同步计划明细增加目标 Component Set 与原 Component Set 标识，使预览能同时识别组件集成员变更和成员移出造成的影响。
- 新增、更新、删除、移动、未变化等非组件集页签继续保持纯文字展示，不创建图片预览。
- Section 最终布局改为以完整外框为目标评分，将顶部标题区域和四周边距纳入长宽比计算；优先选择接近正方形且面积合理的方案。
- Section 内各排内容改为水平居中，并对不均衡行宽和过短尾行增加惩罚，减少左侧堆积、右侧空旷和突兀单项长尾。
- 页面级 Section 布局改为在发生结构变化时统一整理当前资源库的全部托管 Section，不再只摆放新建或待修复 Section。
- 页面级 Section 排版改为基于面积的二维矩形装箱：先按面积从大到小稳定排序，再让较小 Section 回填前面可容纳的剩余矩形区域，避免固定行高造成的大面积空隙。
- 托管 Section 保留固定 `200` 安全间距并从左上角开始；整体比例仅作柔性约束，不会为了硬凑正方形拉散内容。非插件管理的手工 Section 保持原位，托管布局自动选择放在其下方或右侧。

### 性能

- 变动预览每批只渲染 `80` 条记录，并提供“加载更多”；“全部变动”默认不渲染未变化资源，避免大型资源库打开预览时卡顿。
- 组件集缩略图只在用户展开标题后创建，每批加载 `48` 个成员；收起、切换页签或退出预览时立即释放临时图片地址。
- 异常检查先用文件体积、尺寸、同尺寸中位数和单位像素体积筛选候选，只以最多 `3` 路并发读取候选 PNG 的区块信息，不解码图片，也不全量读取正常小文件。
- Section 内布局只评估一组有界候选宽度；页面级二维装箱同样使用有限宽度候选，避免无限搜索并保持确定性排版成本。

### 修复

- 修复首次导入新文件夹后切换到 AI 分类，仍沿用导入前旧索引并把现有资源重复判断为“新增”的问题。
- 页面刷新和分类切换会作废旧 AI 请求，避免旧分类方案应用到新的页面状态。
- 阻止大小差距超过 `1.7506` 倍的资源被 AI 误合并到同一个 Component Set；超限时在画布变更前给出明确错误。
- 修复 Figma 遗留的 `组件集名/资源名` 独立 Component 无法按本地资源名接管的问题；现在会读取末级资源名、复用原 Component ID 并纳入正常分组。
- 修复单个未匹配 Component 位于旧的远端坐标时，把其余托管内容整体推向右侧并导致 Section 出现大面积前置空白的问题；排版前会统一归一化全部直属内容的左上边界。
- 修复旧 Section 子节点元数据缺失时跳过排版修复的问题；选中的异常 Section 现在会被直接加入最终排版，不再依赖某个资源动作间接触发。
- Section 直属的 Component 和 Component Set 无论是否带有插件元数据，都会作为资源参与紧凑排版，游离组件不再被留在原坐标。

### 文档与资源

- 新增 [《Image Folder Importer 使用教程》](使用教程.md)，覆盖安装、文件夹准备、差异概念、严格尺寸与 AI 辅助、变动预览、异常资源、同步注意事项和常见问题。
- README 首页增加使用教程入口。

### 验证

- 增加同步完成后切换 AI 分类不得重复新增现有资源的回归测试。
- 增加“语义名称禁止改写”和“尺寸占位名允许语义化改名”的回归测试。
- 增加成员完全不变的纯尺寸组件集原地改名测试，验证无图片传输且 Component Set、Component ID 和坐标均保持不变。
- 增加 AI 组件集尺寸差距超限的回归测试，并验证分类请求不再包含形状限制。
- 增加同步明细尺寸与移动前路径断言，以及变动预览的筛选、搜索、返回和窄窗口 UI 冒烟验证。
- 增加组件集目标信息、默认收起、展开后图片预览、收起后释放，以及其他页签无图片的回归验证。
- 增加异常元数据 PNG 的识别、体积展示、全部本地资源计数、纯文字列表和窄窗口布局验证。
- 将混合尺寸 Component Set 的 Section 长宽比回归标准从 `≤1.8` 收紧到 `≤1.3`，并验证每排资源相对整体内容水平居中。
- 更新“新增文件夹”回归规则，验证新旧托管 Section 会共同参与页面级排版；使用六个不同尺寸的 Section 验证整体不过分狭长、Section 之间保持 `200` 安全间距、最大 Section 锚定左上角且手工 Section 不移动。
- 增加二维装箱空隙回填测试，验证较小 Section 能进入前面大 Section 组合留下的可用区域，而不是被固定行高推到新行。
- 扩展纯图片替换回归测试，验证没有 Section 级结构变化时，多个已有托管 Section 仍保持原位。
- 增加层级名称旧 Component 原地接管、未托管直属 Component 参与紧凑排版，以及子节点元数据缺失时仍强制修复 Section 的回归测试。
- 当前自动化检查共 58 项，全部通过。

## 2026-07-23 - AI 辅助分类与单 Property 规范

### 新增

- AI 分类请求增加当前 Figma 文件标识、页面 ID、页面名称和可直接定位的页面链接。
- AI 请求增加严格尺寸候选、名称模板语义候选和双向审核规则，辅助发现同尺寸异类与跨尺寸同系列资源。
- 生成 MCP 请求后的 AI 分类状态会显示语义候选系列数量。

### 修改

- AI 回写中的组件集成员只需要提供资源路径。
- 每个 Component Set 只允许一个 Variant Property；AI 可以调整 Property 名。
- 每个变体值固定使用资源名，AI 返回的 `variantValue` 或 `value` 会被忽略。
- AI 未明确调整的资源继续使用严格尺寸分类结果。

### 修复

- 修复 AI 同步后同一 Component Set 可能同时残留两个 Variant Property 的问题。
- 同步时会将旧的多 Property 名称统一为 `Property=资源名`，并修正组件及组件集中的旧分类元数据。
- 继续对资源名中的换行、等号和逗号进行必要的 Figma 语法安全处理，避免非法 Variant 名称。

### 性能

- 多 Property 修复复用同步开始时的组件集名称检查，不在最终排版阶段额外全量扫描大型组件集。
- 保留 MCP Shared Plugin Data 分片传输，避免大型分类请求超过单项数据限制。

### 文档与资源

- 更新 README 中 AI 辅助分类的候选信号和操作流程说明。
- 插件图标替换为科技感的“文件夹导入组件网格”图标，输出尺寸为 128×128，文件为 `icon.png`。
- 新增本更新记录，并约定后续每次修改同步维护。

### 验证

- 增加 AI 页面定位信息、跨尺寸语义候选、单 Property、资源名变体值和旧多 Property 清理测试。
- 当前自动化检查共 48 项，全部通过。
- 大型组件集性能测试保持通过。

## 2026-07-23 - 严格尺寸与 AI 辅助

对应提交：`90675c6`

### 修改

- 删除原“智能规则”分类入口。
- 分类方式调整为严格尺寸，以及基于严格尺寸结果的 AI 辅助。
- AI 只写回分类计划，由插件校验并在用户确认后执行。

## 2026-07-23 - 智能分类归档与增量同步

对应提交：`8fd297f`

### 新增

- 保存智能分类版本作为阶段性基线。
- 完善新增、更新、删除、移动和未变化资源的增量同步处理。

## 2026-07-22 - 增量同步基线

对应提交：`82093cb`

### 新增

- 建立资源扫描、差异预览、增量应用和布局整理的基础能力。

## 2026-07-22 - 初始插件导入

对应提交：`9342bfa`

### 新增

- 将原有 Figma 图片导入插件代码导入仓库主分支。
