# 更新记录

本文件用于持续记录插件的功能调整、问题修复、性能优化、资源更新和验证结果。

## 维护规则

- 每次修改代码、界面、文档或资源时，同步更新本文件。
- 尚未提交的修改记录在“未发布”中。
- 提交或发布时，将“未发布”内容整理为带日期的版本条目。
- 每条记录按需使用：新增、修改、修复、性能、文档与资源、验证。
- 记录用户可感知的结果，并注明重要兼容行为和迁移方式。

## [未发布]

### 新增

- 组件集分类面板增加“刷新页面”按钮，可复用当前 manifest 重新扫描 Figma 页面，无需重新选择本地目录或计算图片哈希。
- 新增独立“变动预览”页面，点击新增、更新、删除、移动或未变化统计卡片即可进入对应明细。
- 预览页支持全部变动、各变动类型、已选一级文件夹和资源名/路径筛选；更新显示前后尺寸，移动显示前后路径，删除显示执行原因。
- 预览页底部可返回同步设置，也可直接确认并开始同步。

### 修改

- 切换严格尺寸与 AI 辅助分类时，会先重新扫描当前 Figma 页面索引，再重新计算同步差异。
- AI 分类请求会携带已有 Component Set 名称及改名策略；只有纯 `宽x高` 尺寸占位名允许 AI 重命名，其他语义名称在读取和执行方案时都会被强制保留。
- AI 分类增加组件尺寸一致性标准：同组线性尺寸比例上限调整为 `1.7506`，取自组件库 AJ 的 `common` Section 中现有 Component Set 的最大实际差距；规则同时写入 MCP 请求并由插件在读取方案时强制校验。
- 删除形状、宽高比和视觉轮廓限制，避免 AI 因定义歧义错误拆组。
- 同步计划明细增加当前尺寸、原尺寸和仅排版修复标记，供变动预览准确解释更新与移动。

### 性能

- 变动预览每批只渲染 `80` 条记录，并提供“加载更多”；“全部变动”默认不渲染未变化资源，避免大型资源库打开预览时卡顿。

### 修复

- 修复首次导入新文件夹后切换到 AI 分类，仍沿用导入前旧索引并把现有资源重复判断为“新增”的问题。
- 页面刷新和分类切换会作废旧 AI 请求，避免旧分类方案应用到新的页面状态。
- 阻止大小差距超过 `1.7506` 倍的资源被 AI 误合并到同一个 Component Set；超限时在画布变更前给出明确错误。

### 验证

- 增加同步完成后切换 AI 分类不得重复新增现有资源的回归测试。
- 增加“语义名称禁止改写”和“尺寸占位名允许语义化改名”的回归测试。
- 增加 AI 组件集尺寸差距超限的回归测试，并验证分类请求不再包含形状限制。
- 增加同步明细尺寸与移动前路径断言，以及变动预览的筛选、搜索、返回和窄窗口 UI 冒烟验证。
- 当前自动化检查共 52 项，全部通过。

## 2026-07-23 - AI 辅助分类与单 Property 规范

### 新增

- AI 分类请求增加当前 Figma 文件标识、页面 ID、页面名称和可直接定位的页面链接。
- AI 请求增加严格尺寸候选、名称模板语义候选和双向审核规则，辅助发现同尺寸异类与跨尺寸同系列资源。
- 生成 MCP 请求后的 AI 分类状态会显示语义候选系列数量。

### 修改

- AI 回写中的组件集成员只需要提供资源路径。
- 每个 Component Set 只允许一个 Variant Property；AI 可以调整 Property 名。
- 每个变体值固定使用资源名，AI 返回的 `variantValue` 或 `value` 会被忽略。
- AI 未明确调整的资源继续使用严格尺寸分类结果。

### 修复

- 修复 AI 同步后同一 Component Set 可能同时残留两个 Variant Property 的问题。
- 同步时会将旧的多 Property 名称统一为 `Property=资源名`，并修正组件及组件集中的旧分类元数据。
- 继续对资源名中的换行、等号和逗号进行必要的 Figma 语法安全处理，避免非法 Variant 名称。

### 性能

- 多 Property 修复复用同步开始时的组件集名称检查，不在最终排版阶段额外全量扫描大型组件集。
- 保留 MCP Shared Plugin Data 分片传输，避免大型分类请求超过单项数据限制。

### 文档与资源

- 更新 README 中 AI 辅助分类的候选信号和操作流程说明。
- 插件图标替换为科技感的“文件夹导入组件网格”图标，输出尺寸为 128×128，文件为 `icon.png`。
- 新增本更新记录，并约定后续每次修改同步维护。

### 验证

- 增加 AI 页面定位信息、跨尺寸语义候选、单 Property、资源名变体值和旧多 Property 清理测试。
- 当前自动化检查共 48 项，全部通过。
- 大型组件集性能测试保持通过。

## 2026-07-23 - 严格尺寸与 AI 辅助

对应提交：`90675c6`

### 修改

- 删除原“智能规则”分类入口。
- 分类方式调整为严格尺寸，以及基于严格尺寸结果的 AI 辅助。
- AI 只写回分类计划，由插件校验并在用户确认后执行。

## 2026-07-23 - 智能分类归档与增量同步

对应提交：`8fd297f`

### 新增

- 保存智能分类版本作为阶段性基线。
- 完善新增、更新、删除、移动和未变化资源的增量同步处理。

## 2026-07-22 - 增量同步基线

对应提交：`82093cb`

### 新增

- 建立资源扫描、差异预览、增量应用和布局整理的基础能力。

## 2026-07-22 - 初始插件导入

对应提交：`9342bfa`

### 新增

- 将原有 Figma 图片导入插件代码导入仓库主分支。
