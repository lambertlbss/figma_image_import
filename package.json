{
  "name": "figma-image-folder-importer",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "check": "node --check code.js && node --test tests/*.test.js",
    "test": "node --test tests/*.test.js"
  }
}
