# Figma 组件库页面排版方法论

## 1. 目标

组件库页面排版的目标不是“把所有内容排成整齐的行”，而是在不破坏设计资产的前提下，同时解决以下问题：

- Section 内部存在大量无效空白。
- 组件组、主组件、实例或 Variants 互相重叠。
- Section 尺寸与内容不匹配，出现越界或空壳容器。
- 整页过长或过宽，浏览、定位和维护成本高。
- 大小不同的 Section 没有合理组合，画布利用率低。

最终结果应满足：结构保持不变、资源保持不变、命名保持不变、内容可读、空间紧凑、无重叠、无越界。

## 2. 操作边界

### 允许修改

- 节点的 `x`、`y` 坐标。
- Section 和 Component Set 的外框宽高。
- 同一父节点下子节点之间的间距和排列顺序对应的位置。
- 页面顶层 Section 之间的组合方式。

### 禁止修改

- 不删除或新增业务资源。
- 不重命名 Page、Section、Component、Component Set、Variant 或普通图层。
- 不改变父子层级，不重新分组，不拆组，不合并组件集。
- 不修改图片、矢量、文本内容、颜色、字体、效果和组件属性。
- 不交换主组件，不解除实例，不修改 Variant 属性。
- 不缩放或旋转资源来迁就排版。

核心原则：**只改变空间关系，不改变资产本身。**

## 3. 三层排版模型

排版应按照从内到外的顺序执行。

### 3.1 Component Set 层

处理对象是 Component Set 下的 Variants。

- 以每个 Variant 的真实宽高作为排版依据。
- 使用紧凑网格或货架式排列，避免固定列宽导致大组件互相覆盖。
- 同一组件集内保持统一间距。
- 排列结束后，使用实际子节点边界重新计算 Component Set 尺寸。
- 不修改 Variant 名称、属性、顺序和内部内容。

推荐间距：

- 小型组件：24–40 px。
- 中大型组件：40–64 px。
- 以最大组件尺寸和可读性为最终判断依据。

### 3.2 Section 层

处理对象是 Section 的直接子节点。

可按照语义类型划分为：

1. Component Set。
2. 主组件 Component。
3. 示例实例 Instance。
4. 其他辅助资源。

每个区域独立排列，区域之间留出更明显的间隔。这样既能减少碰撞，也能保留组件库页面的阅读逻辑。

处理完成后：

- 将内容整体归一到 Section 左上方的统一内边距位置。
- 根据直接子节点的实际最大边界收紧 Section。
- 检查所有直接子节点是否仍在 Section 内。

推荐参数：

- Section 左右内边距：60–100 px。
- Section 顶部内边距：80–120 px。
- 一级组件块间距：60–120 px。
- 不同语义区域间距：100–160 px。

### 3.3 Page 层

处理对象是页面顶层 Section，以及确实存在的顶层独立资源簇。

不建议采用以下简单方案：

- 固定列数网格。
- 固定 Section 宽度。
- 仅按照业务编号顺序纵向堆叠。
- 使用单一货架布局处理极端高矮差异。

推荐使用二维矩形装箱：

- 大 Section 优先形成骨架。
- 小 Section 填补大 Section 周围的空档。
- 不旋转、不缩放 Section。
- 所有 Section 保留统一安全间距。
- 模拟多个目标画布宽度，在长宽比和空间利用率之间选择最佳结果。

## 4. 审计指标

排版前先建立基线，至少统计以下内容。

### 4.1 页面级指标

- 可见顶层节点数量。
- Section 数量。
- 页面可见内容边界：`minX`、`minY`、`maxX`、`maxY`。
- 页面长宽比。
- 顶层节点重叠对数量。

### 4.2 Section 级指标

- 直接子节点数量与类型分布。
- 直接子节点重叠对数量。
- 子节点越界数量。
- 内容包围盒与 Section 面积的比例。
- 空白率和极端宽高比。

### 4.3 Component Set 级指标

- Variant 数量。
- Variant 重叠对数量。
- Variant 越界数量。
- Component Set 内容密度。

## 5. 布局算法

### 5.1 组件集内部：尺寸感知货架布局

基本过程：

1. 读取所有 Variant 的真实宽高。
2. 按已有文档顺序或名称的自然顺序排列。
3. 根据 Variant 总面积估算目标行宽。
4. 当前行无法容纳下一个 Variant 时换行。
5. 每行高度取该行最高 Variant 的高度。
6. 使用实际最大右边界和下边界调整 Component Set 外框。

目标宽度可参考：

```text
targetWidth = max(
  maxChildWidth,
  sqrt(totalChildArea × desiredAspect) × efficiencyFactor
)
```

其中 `desiredAspect` 可以取 3–4，使组件集呈现偏横向、便于浏览的形态。

### 5.2 Section 内部：分区货架布局

先按节点类型分区，再对每个分区执行尺寸感知货架布局。每个分区排完后，以其实际底部位置作为下一个分区的起点。

这种方法可以避免：

- 巨型背景组件和小图标混在同一行。
- 主组件与实例示例相互穿插。
- 使用统一列宽造成大组件覆盖小组件。

### 5.3 整页布局：MaxRects 二维装箱

整页推荐使用 MaxRects 或同类二维矩形装箱算法。

基本过程：

1. 将每个 Section 的宽高加上安全间距，作为待装箱矩形。
2. 按面积或最大边长从大到小排序。
3. 从一个目标画布宽度开始，寻找能够容纳当前 Section 的最佳空闲矩形。
4. 放置后将空闲矩形拆分，并移除完全被其他空闲矩形包含的区域。
5. 计算最终页面宽高、长宽比和空间利用率。
6. 对多个目标宽度重复模拟，选择综合评分最低的结果。

推荐评分模型：

```text
aspectPenalty = abs(log(pageWidth / pageHeight))
wastePenalty  = 1 - contentArea / pageArea
score         = aspectPenalty × aspectWeight + wastePenalty
```

其中：

- `aspectWeight` 通常取 1.5–2.5。
- 页面希望接近方形时，提高 `aspectWeight`。
- 更关注紧凑度时，提高空白率的惩罚权重。

## 6. 参数选择

参数不应直接照搬，应根据当前 Page 的素材尺度计算。

### 页面安全间距

- 小型组件库：160–240 px。
- 中大型游戏素材库：240–400 px。
- Section 标题很大或需要明显分隔时，可使用 400 px 以上。

### 页面目标长宽比

- 推荐范围：0.85–1.2。
- 0.95–1.05 最接近方形，适合整体浏览。
- 如果团队更习惯横向浏览，可放宽到 1.2–1.5。

### 空间利用率

- 70% 以下：通常仍存在明显大空洞。
- 70%–80%：对尺寸差异较大的 Section 属于合理范围。
- 80% 以上：较紧凑，但必须确认 Section 标题和操作空间没有被挤压。

## 7. 验证流程

排版完成后必须进行结构验证和视觉验证。

### 7.1 结构验证

至少检查：

- 顶层节点重叠对为 0。
- Section 直接子节点重叠对为 0，或仅存在确认过的有意叠层。
- Section 直接子节点越界为 0。
- Component Set 内 Variant 重叠对为 0。
- Component Set 内 Variant 越界为 0。
- 节点总数、节点 ID、名称和父子关系与排版前一致。

### 7.2 视觉验证

检查整页截图和问题最严重的典型 Section：

- 页面是否仍然明显过长或过宽。
- 是否存在大片连续空白。
- 小 Section 是否有效填补大 Section 周围空档。
- Section 之间是否保持足够辨识度。
- 组件是否因为排版变得难以对比或浏览。

## 8. 完整执行流程

```text
1. 记录页面结构与节点身份信息
2. 审计页面、Section、Component Set 的重叠和空白
3. 仅整理确实存在问题的 Component Set
4. 整理 Section 的一级组件块
5. 收紧 Section 和 Component Set 边界
6. 基于最终 Section 尺寸模拟多个整页装箱方案
7. 选择长宽比和利用率更优的方案并写入坐标
8. 执行重叠、越界、结构一致性验证
9. 截图进行整页和典型 Section 视觉复检
```

## 9. 验收清单

- [ ] 未删除任何资源。
- [ ] 未新增无关资源。
- [ ] 未修改任何节点名称。
- [ ] 未修改父子层级和组件关系。
- [ ] 未修改文本、图片、矢量、样式和组件属性。
- [ ] 仅修改了坐标、间距和必要的容器宽高。
- [ ] 页面顶层无重叠。
- [ ] Section 内容无越界。
- [ ] Component Set 的 Variants 无重叠、无越界。
- [ ] 页面长宽比处于可接受范围。
- [ ] 页面没有明显的大面积无效空白。
- [ ] 整页截图和典型 Section 截图均通过视觉复检。

## 10. 核心结论

组件库排版的通用顺序是：

> **先审计，后局部；先内部，后外部；先收紧容器，后组合整页；最后用数据和截图双重验证。**

真正有效的整理不是把内容机械对齐，而是在保护设计资产身份和语义结构的前提下，让不同尺寸的内容形成稳定、紧凑且可维护的空间系统。
