# Image Folder Importer 使用教程

## 这个插件是做什么的

这个插件可以把本地文件夹里的 PNG 切图批量导入 Figma，并自动整理成：

- Section
- Component
- Component Set（组件集）

以后本地切图有新增、替换、改名、移动或删除时，可以再次选择同一个资源根目录进行同步。插件只处理发生变化的内容，原来没有变化的组件会保持不动。

## 30 秒快速流程

如果插件已经安装好，最常用的操作只有以下几步：

1. 打开需要存放资源的 Figma 文件和页面。
2. 运行 `Image Folder Importer`。
3. 点击“选择资源根目录”，选择最外层的资源文件夹。
4. 等待扫描完成。
5. 保持默认的“严格尺寸”，检查新增、更新、删除、移动数量。
6. 点击统计数字进入“变动预览”，确认没有异常。
7. 勾选需要同步的文件夹。
8. 点击“开始同步”。
9. 等待出现“同步完成”，不要在中途关闭插件或切换页面。

## 文档里的 Figma 名词

| 名词 | 可以简单理解为 |
|---|---|
| Section | Figma 画布上的一个资源分类区域 |
| Component | 一个可以复用的组件资源 |
| Component Set | 一组相关组件，例如同一图标的不同状态 |
| Variant | 组件集里的一个具体成员或状态 |
| 资源根目录 | 包住所有分类文件夹的最外层本地文件夹 |
| 相对路径 | 从资源根目录往下看的文件位置，例如 `common/button/confirm.png` |

## 第一次使用前

### 安装开发版插件

首次安装只需要做一次：

1. 在 Figma 桌面版中打开任意设计文件。
2. 打开 Figma 菜单。
3. 进入 `Plugins → Development → Import plugin from manifest…`。
4. 在插件项目文件夹中选择 `manifest.json`。
5. 安装完成后，可以在 `Plugins → Development` 中找到 `Image Folder Importer`。

如果看不到 `Development`，请确认使用的是 Figma 桌面版，并让项目维护人员确认你拿到的是完整插件文件夹。

### 准备本地资源目录

插件只读取 PNG 文件。推荐目录结构如下：

```text
res/
├─ common/
│  ├─ home.png
│  ├─ close.png
│  └─ button/
│     ├─ confirm.png
│     └─ cancel.png
├─ battle/
│  ├─ attack.png
│  └─ defend.png
└─ item/
   ├─ potion.png
   └─ coin.png
```

这里：

- `res` 是资源根目录，也是点击“选择资源根目录”时要选择的文件夹。
- `common`、`battle`、`item` 是根目录下的第一级文件夹。
- 插件会为这三个一级文件夹分别创建或复用同名 Section。
- `common/button` 这样的更深层文件夹只保留在资源路径中，不会继续创建 Section。

### 文件命名注意事项

- 同一个一级文件夹对应的 Section 中，资源名应尽量唯一。
- 文件名去掉 `.png` 后，就是 Figma 中的组件名。
- 如果同一个 Section 中出现同名资源，插件会保留本地最新的那一个，用新资源替换旧组件。
- 不建议使用只有空格、换行或大量特殊符号的文件名。
- 不要为了区分状态随意改动整个文件夹层级；移动路径可能被识别为“移动”操作。

## 完整操作流程

### 第一步：打开正确的 Figma 页面

先打开最终要接收资源的 Figma 文件和页面，再运行插件。

扫描完成后不要切换到其他页面再同步，因为插件的差异判断是针对扫描时的当前页面生成的。如果页面内容被其他人修改了，请点击插件中的“刷新页面”重新检查。

### 第二步：选择资源根目录

点击“选择资源根目录”，选择包含所有一级分类文件夹的最外层目录，例如上面的 `res`。

插件会依次完成：

1. 读取 PNG 文件名、路径、尺寸和体积。
2. 检查异常大的资源和可疑 PNG 元数据。
3. 对比当前 Figma 页面中的已有资源。
4. 生成新增、更新、删除、移动和未变化清单。

资源很多时请耐心等待。扫描期间不要重复点击按钮。

### 第三步：看懂五种变动

| 类型 | 表示什么 | Figma 中会发生什么 |
|---|---|---|
| 新增 | 本地有，当前页面中没有 | 创建新的 Component |
| 更新 | 路径能对应，但 PNG 文件指纹或尺寸发生变化 | 用本地新图片替换旧图片，尽量保留原 Component |
| 删除 | Figma 中有，但本地已经不存在 | 勾选删除同步后，删除插件管理的旧资源 |
| 移动 | 图片内容相同，但路径、归组或排版需要调整 | 保留 Component，调整路径或所属组件集 |
| 未变化 | 本地文件与 Figma 记录一致 | 不重写、不移动 |

#### 为什么图片看起来没变，却显示“更新”

“更新”比较的是整个 PNG 文件，不只是肉眼看到的画面。

即使画面完全一样，重新导出后以下内容也可能变化：

- PNG 压缩方式
- 色彩配置
- 导出工具写入的元数据
- 文件内部区块排列

这些变化会让文件指纹不同，所以插件会显示“更新”。成功同步后，如果再次选择完全相同的本地文件，通常会变成“未变化”。

### 第四步：选择组件集分类方式

#### 严格尺寸（默认，推荐日常使用）

规则很简单：

- 位于同一个一级文件夹中；
- 图片宽高完全相同；
- 同组至少有两个资源。

满足这些条件的资源会组成 Component Set。只有一个资源时，会保持为普通 Component，不会为了凑组强行创建组件集。

严格尺寸最稳定，第一次使用或不确定时保持默认即可。

#### AI 辅助（可选）

AI 辅助适合需要按语义整理组件集的情况，例如：

- 同样尺寸但实际属于不同系列，需要拆开。
- 不同尺寸但实际属于同一个状态系列，需要合并。
- 希望把纯尺寸组件集名称改成更易理解的名称。

使用前提：组内已经准备好能够连接当前 Figma 文件的 AI/Figma MCP 工具。如果没有这套环境，请继续使用“严格尺寸”。

操作步骤：

1. 将“组件集分类”切换为“AI 辅助（基于严格尺寸）”。
2. 勾选希望 AI 分析的文件夹。
3. 点击“生成 MCP 请求”。
4. 把插件复制出来的提示交给已经连接当前 Figma 文件的 AI 助手。
5. 等待 AI 写回分类方案。
6. 回到插件，点击“读取 AI 方案”。
7. 再次查看差异和“组件集”页签，确认后同步。

AI 只提供分类方案，不会在你点击同步前直接移动组件。

每个组件集最终只保留一个变体属性，具体变体值使用资源名。AI 可以调整组件集名称或属性名称，但不会随意改动资源名。

已有组件集即使成员完全没有变化，只要名称是 `404x404`、`35×36` 这类纯尺寸占位名，也会进入 AI 命名审核。能够判断可靠用途时会优化成语义名称；无法可靠判断时保留原名。仅修改名称不会重建组件集或更换内部组件 ID。

如果 AI 把尺寸差距过大的资源放进同一个组件集，插件会拒绝该方案，并提示重新拆分。

### 第五步：选择需要同步的文件夹

“文件夹分类”区域列出了本次扫描涉及的一级文件夹。

- 勾选：本次会处理该文件夹。
- 取消勾选：本次保持该文件夹不动。
- “全选”：一次勾选全部文件夹。
- “全取消”：一次取消全部文件夹。

不确定时可以只同步一两个文件夹，确认结果后再处理其他文件夹。

### 第六步：决定是否同步删除

只有检测到本地已删除资源时，插件才会显示“同步本地删除操作”。

- 勾选：删除 Figma 中由插件管理、但本地已经不存在的资源。
- 取消勾选：本次保留这些 Figma 资源。

同名重复组件属于资源冲突清理，不一定受这个选项控制。正式同步前请查看“删除”页签中的具体原因。

### 第七步：查看变动预览

点击新增、更新、删除、移动或未变化统计卡片，可以进入“变动预览”。

#### 全部变动

显示本次会执行的新增、更新、删除、移动和冲突，不显示大量未变化资源。

#### 组件集

显示本次受到影响的组件集，以及同步完成后预计保留的内部资源。

- 默认收起。
- 点击组件集标题向下展开。
- 只有这个页签会显示本地图片缩略图。
- 新增、更新、移动等标签表示该成员本次会发生什么。
- “保留”表示它没有变化，但因为同组其他资源发生变化，所以一起展示最终结果。

这里展示的是“同步后的预计结果”。如果同步失败，Figma 页面可能还没有变成预览中的状态。

#### 异常资源

异常资源页签检查全部有效本地 PNG，不受文件夹是否勾选影响。

每项会显示：

- 文件路径
- 实际文件体积
- 图片尺寸
- 单位像素体积
- 相同尺寸资源的体积中位数
- 可疑元数据大小及占比
- 异常原因

风险含义：

- “高风险”：发现明显偏大的 PNG 元数据，或者 PNG 内部区块无法正常读取。
- “需检查”：文件体积明显偏离同尺寸资源，或文件本身特别大，但不一定真的有问题。

异常检查只负责提醒：

- 不会自动删除元数据。
- 不会修改本地 PNG。
- 不会阻止同步。
- 不会改变新增、更新、删除、移动的判断。

发现高风险资源时，建议回到原始设计文件重新导出 PNG，并关闭不必要的元数据写入。替换本地文件后重新选择资源根目录，确认异常提示消失。

#### 其他页签

新增、更新、删除、移动、未变化页签只显示文字信息，不加载图片预览，避免资源很多时卡顿。

所有页签都可以：

- 按文件夹筛选。
- 搜索资源名或路径。
- 点击“加载更多”查看后续内容。

### 第八步：开始同步

确认无误后，可以：

- 返回主界面点击“开始同步”；或
- 在变动预览底部点击“确认同步”。

同步期间：

- 不要关闭插件。
- 不要切换 Figma 页面。
- 不要删除或移动正在处理的组件。
- 不要让其他人同时在同一页面大规模整理这些资源。

同步完成后，插件会显示新增、更新、删除、移动数量和耗时。

## 后续日常更新

以后本地资源发生变化时，重复以下流程即可：

1. 打开原来的 Figma 文件和资源页面。
2. 运行插件。
3. 再次选择同一个资源根目录。
4. 检查变动数量和异常资源。
5. 选择要处理的一级文件夹。
6. 确认后同步。

未变化的 Component 和 Component Set 不会每次全部重建。纯图片替换也不会触发排版；但新增、删除或尺寸变化影响 Section 结构时，插件会重新整理当前资源库的全部托管 Section，让整个页面级布局保持整齐。

如果你在扫描后手工修改了当前 Figma 页面，不需要重新选择本地目录，可以点击“刷新页面”重新生成差异。

## 排版和命名规则

### Section

- 只使用资源根目录下的第一级文件夹创建 Section。
- 当前页面已经存在唯一的同名 Section 时，插件会优先复用。
- Section 中不属于插件管理的手工内容不会被删除或重写；直属 Component 和 Component Set 即使缺少旧版插件记录，也会一起参与资源排版，避免单个游离组件造成大面积前置空白。
- Figma 遗留的 `组件集名/资源名` 层级 Component 会按最后一级资源名匹配本地资源，匹配成功后复用原 Component ID 并纳入正常排版。
- Section 内的组件集和组件会自动整理，间距为 100。
- 最终外框会把标题区域和四周边距一起计算，整体尽量接近正方形。
- 每排资源会水平居中，避免最后一排贴在左侧或只留下突兀的单项长尾。
- 多个由插件管理的 Section 之间保留固定 200 安全间距，并作为一个整体排成紧凑的页面级布局。
- 插件会先按 Section 面积从大到小稳定排序，让较大的内容先确定整体骨架。
- 随后的较小 Section 会优先回填前面能够容纳的空区，不再受固定行高限制，因此不会明明放得下却留下大片空白。
- 页面整体从左上角开始，只避免过分狭长，不会为了严格凑成正方形而制造大面积空白。
- 不属于当前插件资源库的手工 Section 不会被移动；托管 Section 布局会放在其下方或右侧，避免重叠。

### Component Set

- 内部资源会尽量排成接近方形的紧凑网格。
- 外框会适配内部内容，不会故意拉成长条。
- 组件集统一使用 1 像素、内部、`#8A38F5` 描边。
- 组件集只有至少两个资源时才会存在。

### Variant 名称

Figma 要求变体名使用“属性=值”的格式。插件会自动使用类似以下名称：

```text
Property 1=home
Property 1=close
```

旧版遗留的不合法名称会在同步时自动修复。

## 常见问题

### 扫描或同步一直不动

先观察进度文字是否仍在变化。大型资源库可能需要一些时间。

如果长时间完全没有变化：

1. 不要连续重复点击同步。
2. 关闭插件后重新打开。
3. 确认仍在原来的 Figma 页面。
4. 重新选择资源根目录。
5. 尝试只勾选较少的一级文件夹同步。

### 提示“插件响应超时”

通常表示当前页面或资源量太大，处理没有在规定时间内完成。

可以：

- 点击“刷新页面”后重新检查。
- 减少本次勾选的文件夹。
- 检查异常资源页是否存在体积特别大的 PNG。
- 避免同步期间同时大规模操作 Figma 页面。

### 提示某个节点不存在

说明扫描后，Figma 中对应组件可能被删除、撤销或被其他操作改变。

点击“刷新页面”重新生成差异，再执行同步。不要继续使用旧的预览结果。

### 同一个 Section 中仍然出现重名资源

先点击“刷新页面”重新扫描。如果仍然存在：

- 确认本地同一个一级文件夹中是否有多个同名 PNG。
- 查看“删除”页签是否出现“同名重复 Component”。
- 确认同步时没有取消必要的冲突清理。

插件的目标是同一个 Section 中同名资源只保留本地最新版本。

### 组件集没有创建

常见原因：

- 分类后只有一个资源。
- 严格尺寸模式下，图片宽高并不完全相同。
- AI 方案把该资源设为独立组件。

只有同组至少两个资源时才会创建 Component Set。

### AI 方案读取失败

检查：

- AI 是否连接了当前这个 Figma 文件。
- 是否在生成请求后切换了页面或重新扫描。
- AI 是否引用了未勾选或不存在的资源。
- AI 是否把尺寸差距过大的资源放进同一个组件集。

页面或本地清单变化后，旧 AI 方案会失效，需要重新生成 MCP 请求。

### 异常资源一定有问题吗

不一定。

“高风险”通常值得重新导出检查；“需检查”可能只是图片本身尺寸大、内容复杂或压缩率较低。可以结合原图用途判断，不要只看文件体积就直接删除。

## 同步前检查清单

每次正式同步前，可以快速确认：

- [ ] 当前打开的是正确的 Figma 文件和页面。
- [ ] 选择的是正确的资源根目录。
- [ ] 一级文件夹与预期 Section 分类一致。
- [ ] 已查看新增、更新、删除和移动数量。
- [ ] 已检查“组件集”中的同步后结果。
- [ ] 已检查“异常资源”中的高风险文件。
- [ ] 已确认是否同步本地删除操作。
- [ ] 已选择正确的文件夹。
- [ ] 同步期间没有其他人同时整理同一批资源。

## 给项目维护人员反馈问题时

为了更快定位问题，请尽量提供：

1. 插件界面完整截图。
2. 当前停在哪个步骤。
3. 页面上的错误文字。
4. 大约有多少个 PNG 和多少个一级文件夹。
5. 出问题资源的相对路径和文件体积。
6. 问题发生在严格尺寸还是 AI 辅助模式。
7. 是否点击过“刷新页面”并重新尝试。

请不要只截图 Figma 画布，插件中的进度和错误信息同样重要。
